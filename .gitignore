# --- Dependencies (Thư viện nặng - Máy khác tự cài lại được) ---
node_modules/
**/node_modules/

# --- Environment & Secrets (Bảo mật tuyệt đối) ---
.env
*.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- OS & Tooling (File rác hệ thống) ---
.DS_Store
Thumbs.db
.idea/
.vscode/
*.log

# --- Expo & React Native Build (File build tạm) ---
.expo/
dist/
web-build/
*.jks
*.keystore
*.pem

# --- Android/iOS Generated (Nếu sau này build native) ---
# android/
# ios/
# (Tạm thời giữ lại folder android/ios nếu bạn eject, nhưng hiện tại Expo Go chưa cần chặn)

# --- Utility Scripts (File gộp code dùng riêng) ---
merge_code.js
TOAN_BO_SOURCE_CODE.txt