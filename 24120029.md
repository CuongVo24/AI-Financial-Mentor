# BÀI TẬP TÌM HIỂU VÀ SỬ DỤNG MERMAID.LIVE
- **Học phần:** Kiến trúc Phần mềm / Nhập môn Công nghệ Phần mềm
- **Chủ đề thực hành:** Quy trình đặt hàng và thanh toán trực tiếp (No E-Wallet) - Dự án UniVillage

---

### 1. Flowchart (Mô tả thuật toán đặt hàng và kiểm tra tồn kho)
Biểu đồ này mô tả thuật toán kiểm tra số lượng sản phẩm trong kho và hướng dẫn người dùng phương thức thanh toán không qua ví điện tử.

```mermaid
flowchart TD
    A[Bắt đầu] --> B(Người dùng nhấn Đặt hàng)
    B --> C{Kiểm tra tồn kho}
    C -->|Hết hàng| D[Báo lỗi & Yêu cầu cập nhật]
    D --> B
    C -->|Còn hàng| E{Chọn phương thức thanh toán}
    E -->|Tiền mặt - COD| F[Xác nhận đơn COD]
    E -->|Chuyển khoản trực tiếp| G[Hiển thị STK của người bán]
    F --> H[Lưu đơn hàng vào Cơ sở dữ liệu]
    G --> H
    H --> I[Kết thúc]
```

---

### 2. Sequence Diagram (Mô tả thứ tự thực hiện công việc tạo đơn hàng)
Biểu đồ thể hiện trình tự giao tiếp giữa các thành phần trong hệ thống khi có luồng công việc đặt mua sản phẩm.

```mermaid
sequenceDiagram
    actor Student as Sinh viên mua
    participant App as Ứng dụng Mobile (Flutter)
    participant API as Backend (NestJS)
    participant DB as Database (PostgreSQL)

    Student->>App: Chọn sản phẩm & Bấm mua
    App->>API: Gửi request tạo đơn hàng
    API->>DB: Truy vấn kiểm tra số lượng
    DB-->>API: Trả về kết quả tồn kho
    
    alt Hết hàng
        API-->>App: Trả về lỗi 400 (Out of stock)
        App-->>Student: Hiển thị popup sản phẩm đã hết
    else Hợp lệ
        API->>DB: Insert dữ liệu đơn hàng mới
        DB-->>API: Trả về kết quả Insert thành công
        API-->>App: Trả về thông tin đơn hàng
        App-->>Student: Chuyển hướng sang màn hình Thành công
    end
```

---

### 3. Architecture (Mô tả kiến trúc hệ thống tổng thể)
Sơ đồ phân rã kiến trúc 3 tầng của ứng dụng UniVillage.

```mermaid
flowchart LR
    subgraph Client_Tier [Tầng Client]
        MobileApp["Ứng dụng Mobile (Flutter)"]
    end
    
    subgraph Logic_Tier [Tầng Logic & Xử lý]
        LoadBalancer["Load Balancer / Nginx"]
        BackendAPI["Backend API (NestJS)"]
    end
    
    subgraph Data_Tier [Tầng Dữ liệu]
        PrimaryDB[("Cơ sở dữ liệu chính (PostgreSQL)")]
        ReplicaDB[("Bản sao lưu (PostgreSQL Replica)")]
    end
    
    MobileApp <-->|HTTPS / REST| LoadBalancer
    LoadBalancer <--> BackendAPI
    BackendAPI <-->|TypeORM / Prisma| PrimaryDB
    PrimaryDB -.->|Đồng bộ dữ liệu| ReplicaDB
```

---

### 4. C4 Model (Mô tả Context - Bối cảnh hệ thống cấp độ 1)
Mô tả mối quan hệ giữa Sinh viên, Nền tảng UniVillage và Ứng dụng ngân hàng bên ngoài.

```mermaid
C4Context
    title Bức tranh toàn cảnh (System Context) của nền tảng UniVillage

    Person(student, "Sinh viên KTX", "Người dùng ứng dụng để mua bán, trao đổi hàng hóa.")
    System(univillage, "Nền tảng UniVillage", "Hệ thống trung tâm quản lý giao dịch và nội dung.")
    System_Ext(bank_app, "Ứng dụng Ngân hàng", "Được sinh viên sử dụng độc lập để chuyển tiền thủ công.")

    Rel(student, univillage, "Đăng sản phẩm, tìm kiếm, đặt hàng", "HTTPS/REST")
    Rel(student, bank_app, "Thực hiện chuyển khoản trực tiếp qua App ngân hàng", "Thủ công")
    Rel(bank_app, student, "Thông báo biến động số dư", "Notification")
```
